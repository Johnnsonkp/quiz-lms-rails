// import { useCallback } from 'react';

// interface Quiz {
//   id: number;
//   title: string;
// }

// export const getQuizId = useCallback((title: string): number | null => {
//     const quiz = quizList?.find((q: Quiz) => q.title === title);
//     return quiz?.id || null;
// }, [quizList]);